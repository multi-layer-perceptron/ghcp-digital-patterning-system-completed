# C++ Pattern Processor

The C++17 pattern processor models image validation, metadata extraction, palette extraction, channel mapping, grid conversion, and machine command generation.

## Validation

When CMake is available, run the commands in the feature quickstart. With only `g++`, individual tests can be compiled directly by linking each test with its matching source files.
