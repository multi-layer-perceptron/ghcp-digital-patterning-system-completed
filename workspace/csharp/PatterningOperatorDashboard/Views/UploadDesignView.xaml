<UserControl x:Class="PatterningOperatorDashboard.Views.UploadDesignView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:PatterningOperatorDashboard.Views">
  <UserControl.Resources>
    <local:HexToColorConverter x:Key="HexToColorConverter" />
  </UserControl.Resources>
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Text="Design Upload" FontSize="20" FontWeight="SemiBold" />

    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,0">
      <Button x:Name="BrowseButton" Content="Browse PNG/JPEG..." Padding="12,6" Click="BrowseButton_Click" />
      <Button x:Name="SampleButton" Content="Load Bundled Sample" Padding="12,6" Margin="8,0,0,0" Click="SampleButton_Click" />
      <Button x:Name="ResetButton" Content="Reset" Padding="12,6" Margin="8,0,0,0" Click="ResetButton_Click" />
    </StackPanel>

    <TextBlock Grid.Row="2"
               Text="{Binding StatusMessage}"
               Margin="0,8,0,0"
               TextWrapping="Wrap"
               Foreground="#444" />

    <Border Grid.Row="3" BorderBrush="#777" BorderThickness="1" Padding="12" Margin="0,12,0,0">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Left: preview image -->
        <StackPanel Grid.Column="0" Margin="0,0,8,0">
          <TextBlock Text="Preview" FontWeight="SemiBold" />
          <Border BorderBrush="#CCC" BorderThickness="1" Margin="0,6,0,0" MinHeight="200" Background="#FAFAFA">
            <Image Source="{Binding PreviewImage}" Stretch="Uniform" />
          </Border>
        </StackPanel>

        <!-- Right: metadata + palette -->
        <StackPanel Grid.Column="1" Margin="8,0,0,0">
          <TextBlock Text="Concept" FontWeight="SemiBold" />
          <StackPanel Margin="0,4,0,0" DataContext="{Binding Concept}">
            <TextBlock>
              <Run Text="Source: " FontWeight="SemiBold" /><Run Text="{Binding SourceName, FallbackValue=—}" />
            </TextBlock>
            <TextBlock>
              <Run Text="MIME: " FontWeight="SemiBold" /><Run Text="{Binding MimeType, FallbackValue=—}" />
            </TextBlock>
            <TextBlock>
              <Run Text="Size (bytes): " FontWeight="SemiBold" /><Run Text="{Binding FileSizeBytes, FallbackValue=—}" />
            </TextBlock>
            <TextBlock>
              <Run Text="Status: " FontWeight="SemiBold" /><Run Text="{Binding AnalysisStatus, FallbackValue=—}" />
            </TextBlock>
          </StackPanel>

          <TextBlock Text="Metadata" FontWeight="SemiBold" Margin="0,12,0,0" />
          <StackPanel Margin="0,4,0,0" DataContext="{Binding Metadata}">
            <TextBlock>
              <Run Text="Width × Height: " FontWeight="SemiBold" />
              <Run Text="{Binding WidthPx, FallbackValue=—}" />
              <Run Text=" × " />
              <Run Text="{Binding HeightPx, FallbackValue=—}" />
            </TextBlock>
            <TextBlock>
              <Run Text="Aspect ratio: " FontWeight="SemiBold" /><Run Text="{Binding AspectRatio, FallbackValue=—}" />
            </TextBlock>
            <TextBlock>
              <Run Text="Estimated unique colors: " FontWeight="SemiBold" /><Run Text="{Binding EstimatedUniqueColors, FallbackValue=—}" />
            </TextBlock>
            <TextBlock>
              <Run Text="Transparency: " FontWeight="SemiBold" /><Run Text="{Binding HasTransparency, FallbackValue=—}" />
            </TextBlock>
          </StackPanel>

          <TextBlock Text="Palette" FontWeight="SemiBold" Margin="0,12,0,0" />
          <ItemsControl ItemsSource="{Binding PaletteColors}" Margin="0,4,0,0">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <StackPanel Orientation="Horizontal" Margin="0,2,0,2">
                  <Border Width="20" Height="20" BorderBrush="#444" BorderThickness="1">
                    <Border.Background>
                      <SolidColorBrush Color="{Binding Hex, Converter={StaticResource HexToColorConverter}}" />
                    </Border.Background>
                  </Border>
                  <TextBlock Margin="8,0,0,0" VerticalAlignment="Center">
                    <Run Text="{Binding Label}" FontWeight="SemiBold" />
                    <Run Text=" " />
                    <Run Text="{Binding Hex}" />
                    <Run Text=" — " />
                    <Run Text="{Binding CoveragePercent, StringFormat={}{0:0.0}%}" />
                  </TextBlock>
                </StackPanel>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </StackPanel>
      </Grid>
    </Border>
  </Grid>
</UserControl>
