<UserControl x:Class="PatterningOperatorDashboard.Views.ChannelMappingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <TextBlock Text="Channel Mapping" FontSize="20" FontWeight="SemiBold" />
    <ListView Grid.Row="1" Margin="0,12,0,0" ItemsSource="{Binding Channels}">
      <ListView.View>
        <GridView>
          <GridViewColumn Header="No." DisplayMemberBinding="{Binding ChannelNumber}" />
          <GridViewColumn Header="Label" DisplayMemberBinding="{Binding Label}" />
          <GridViewColumn Header="Color" DisplayMemberBinding="{Binding HexColor}" />
        </GridView>
      </ListView.View>
    </ListView>
  </Grid>
</UserControl>
