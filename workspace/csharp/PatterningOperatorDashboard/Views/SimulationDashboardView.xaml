<UserControl x:Class="PatterningOperatorDashboard.Views.SimulationDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <!-- Header -->
    <Grid Grid.Row="0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <TextBlock Text="Production Simulation" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" />
      <Border Grid.Column="1" Background="#1F2937" CornerRadius="4" Padding="10,4">
        <TextBlock Text="{Binding StatusBadge}" Foreground="White" FontWeight="SemiBold" />
      </Border>
    </Grid>

    <!-- Controls -->
    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,12">
      <Button x:Name="StartButton" Content="Start" Width="80" Margin="0,0,8,0"
              IsEnabled="{Binding CanStart}" Click="StartButton_Click" />
      <Button x:Name="PauseButton" Content="Pause" Width="80" Margin="0,0,8,0"
              IsEnabled="{Binding CanPause}" Click="PauseButton_Click" />
      <Button x:Name="ResumeButton" Content="Resume" Width="80" Margin="0,0,8,0"
              IsEnabled="{Binding CanResume}" Click="ResumeButton_Click" />
      <Button x:Name="ResetButton" Content="Reset" Width="80"
              IsEnabled="{Binding CanReset}" Click="ResetButton_Click" />
    </StackPanel>

    <!-- Status banner -->
    <Border Grid.Row="2" Background="#F3F4F6" CornerRadius="4" Padding="12,8" Margin="0,0,0,8">
      <StackPanel>
        <TextBlock Text="{Binding StatusMessage}" FontWeight="SemiBold" TextWrapping="Wrap" />
        <TextBlock Text="{Binding GridSummary}" Foreground="#374151" Margin="0,4,0,0" />
      </StackPanel>
    </Border>

    <!-- Progress -->
    <Grid Grid.Row="3" Margin="0,0,0,8">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <ProgressBar Height="18" Minimum="0" Maximum="100" Value="{Binding ProgressPercent, Mode=OneWay}" />
      <TextBlock Grid.Column="1" Text="{Binding PassLabel}" Margin="12,0,0,0" VerticalAlignment="Center" />
    </Grid>

    <!-- Diagnostics -->
    <GroupBox Grid.Row="4" Header="Diagnostics" Margin="0,0,0,8">
      <StackPanel>
        <TextBlock Text="{Binding DiagnosticsSummary}" Margin="4,4,4,6" />
        <ItemsControl ItemsSource="{Binding Diagnostics}">
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <Border Background="#FEF2F2" BorderBrush="#FCA5A5" BorderThickness="1"
                      CornerRadius="3" Padding="8,4" Margin="4,2">
                <StackPanel Orientation="Horizontal">
                  <TextBlock Text="{Binding Severity}" FontWeight="Bold" Foreground="#B91C1C" Width="70" />
                  <TextBlock Text="{Binding Category}" Width="100" Foreground="#374151" />
                  <TextBlock Text="{Binding Message}" TextWrapping="Wrap" />
                </StackPanel>
              </Border>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </StackPanel>
    </GroupBox>

    <!-- Event log -->
    <GroupBox Grid.Row="5" Header="Event Log">
      <ListView ItemsSource="{Binding Events}">
        <ListView.View>
          <GridView>
            <GridViewColumn Header="#" Width="40" DisplayMemberBinding="{Binding Sequence}" />
            <GridViewColumn Header="Time" Width="110" DisplayMemberBinding="{Binding Timestamp, StringFormat=HH:mm:ss.fff}" />
            <GridViewColumn Header="Type" Width="80" DisplayMemberBinding="{Binding EventType}" />
            <GridViewColumn Header="Channel" Width="100" DisplayMemberBinding="{Binding ChannelId}" />
            <GridViewColumn Header="Message" Width="500" DisplayMemberBinding="{Binding Message}" />
          </GridView>
        </ListView.View>
      </ListView>
    </GroupBox>
  </Grid>
</UserControl>

