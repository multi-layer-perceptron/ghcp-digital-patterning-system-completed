# FPGA Stub

This folder contains portable VHDL-2008 simulation stubs for channel signal mapping. The artifacts are workshop validation stubs and have no synthesis target.

## Validation

When GHDL is available, analyze `signal_map.vhd` and `signal_map_tb.vhd` to confirm the timing-valid output responds to channel activity.
