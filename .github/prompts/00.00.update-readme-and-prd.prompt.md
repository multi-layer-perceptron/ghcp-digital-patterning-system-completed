---
name: 00.00.update-readme-and-prd
description: "Update the README.md and Digital Patterning PRD to reflect the current simulator, validation paths, and workshop guidance."
agent: agent
model: GPT-5.5 (copilot)
---

# Update README.md and Digital Patterning PRD

First, recursively examine the repository contents, excluding generated folders and build outputs, to identify changes made
since `README.md` and `docs/prd-specify-digital-patterning-system.md` were last updated.

Update `README.md` to reflect the current Digital Patterning System Simulator, including setup paths, prerequisites,
validation commands, project layout, useful commands, and any lab prompt inventory. If prompt files are listed, order them
primarily by numeric prefix such as `00.00`, `02.01`, `04.00`, and `10.05`.

Update `docs/prd-specify-digital-patterning-system.md` with matching product, requirement, technical approach, testing,
risk, decision, validation, and appendix changes. Preserve the PRD's product-oriented tone and keep it aligned with the
real simulator stack:

- C#/.NET 8 domain services, xUnit tests, WPF operator dashboard, and gateway host under `workspace/csharp/`
- C++17 pattern processor under `workspace/cpp/`
- C11 control emulator under `workspace/control-c/`
- SQL Server-compatible contracts and SQLite validation under `workspace/sql/`
- PLC Structured Text stubs under `workspace/plc/`
- FPGA VHDL stubs under `workspace/fpga/`
- Generic sample assets under `workspace/assets/samples/`

Capture caveats, gotchas, and lessons learned from recent implementation or validation work, especially setup,
Codespaces/devcontainer behavior, Windows-only WPF dashboard constraints, CMake/CTest validation, SQL container
validation, gateway stub usage, and review-workflow issues future participants are likely to hit.

Do not reintroduce unrelated inherited-template language. If the repository contains incomplete or inconsistent updates,
preserve existing files where appropriate and summarize those issues in the final response.